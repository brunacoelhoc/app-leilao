// Modelo do arquivo de configuracao local. Copie para "environment.ts"
// (mesmo nome, sem ".example") e preencha com os valores reais.
// "environment.ts" fica fora do Git (igual ao BackEnd/.env)
export const environment = {
  apiUrl: 'http://localhost:3000/api',
  // Mesma API_KEY configurada em BackEnd/.env
  apiKey: 'cole-aqui-a-mesma-API_KEY-do-BackEnd/.env',
};
